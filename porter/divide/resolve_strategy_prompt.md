## 拆分策略（按需读取）

路径: {strategy_path}

当你需要判断"片段该搬去哪侧"或"环上该断哪条边"时，读此文件。
对解环最有用的内容：

- §5 模块草图表：各模块的职责与代表函数——判断片段归属的依据
- §5 末尾依赖序：模块依赖的自然方向——判断该断哪条边
  （逆向边断、顺向边留）
- §1/§4 的三面划分（数据/控制/诊断面）：符号不在 §5 表中时
  判断功能归属的背景

其余章节与本任务无关，不必读。
