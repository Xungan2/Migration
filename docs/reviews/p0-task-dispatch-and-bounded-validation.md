# P0 任务派发与有界验证审查

审查基点：`b952976c17d253a8137485a8ee5d4bde0bdcd9af`。范围为本次规格、任务执行与提示、公开 CLI 集成测试及使用说明；两轴由独立 agent 审查。

## Standards

未发现未解决的标准问题。实现使用标准库和现有公开 CLI 测试入口，维持宿主顺序派发、单一代码写入者与知识 agent 单一写入者。

通用任务约束由基础 task skill 维护，类别 skill 保留专属步骤。主 agent 根据保留证据确认必要目标的接口复用现有验收入口，检查目标与验收状态并保留引用。

## Spec

未发现未解决的规格问题。补充目标的失败与尝试记录可跨续跑保留；混合调用失败时，主 agent 可凭保留规划证据明确验收必要目标，宿主保存执行失败和验收引用，无需重跑已完成规划。

## Validation

- `python3 -m unittest discover -s tests`：30 个测试通过。
- `mypy --check-untyped-defs porter`：5 个源文件检查通过，检查器安装于仓库外。
- `git diff --check` 与暂存差异检查通过。
- 覆盖补充失败与超时延期、重复派发拒绝、必要任务重试条件、分类交付、混合调用失败后的证据验收、异常目标结果、知识保护，以及 SIGINT/SIGKILL 后续跑的一次尝试限制。

确定性替身验证宿主契约；真实模型的派发选择、语义相同目标识别和任务内停止行为仍需实际运行核查。本次没有重新运行真实 P0，也没有将本地构建载入案例当作真实 Flash 设备验证。

未解决项：Standards 0；Spec 0。
