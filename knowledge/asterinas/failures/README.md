# knowledge/asterinas/failures/ — 失败签名（asterinas@docker/osdk/QEMU 环境特定）

通用逻辑形态见 base/failures/；本分区收**绑定本迁移环境**的
签名特征（docker 文案、osdk 命令形态、QEMU 设备行为）。条目
格式同 base（签名/判别/归责/建议动作），技术细节指针指向
本 lineage 的 pitfalls 全文。

INDEX.json 为机器索引（薄格式）；条目文件 = `<id>.md`。
